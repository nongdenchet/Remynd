apply from: '../scripts/module.gradle'

dependencies {
    implementation(
            Libs.kotlin,
            Libs.coroutines,
            Libs.annotation,
            Libs.dagger,
    )

    testImplementation(
            Libs.junit
    )
}
